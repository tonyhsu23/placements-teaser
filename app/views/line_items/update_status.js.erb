<% if @status == "failure" %>
    alert("Invalid status transition")
<% else %>
    $("#lineitem-status-<%= @item.id %>").html("<%= @item.status %>");
    $("#lineitem-adjustments-btn-<%= @item.id %>").attr("disabled", true);
<% end %>
