<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'Placements.io', root_path, class: 'navbar-brand'%>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <%= nav_item do %>
        <%= nav_link 'Line Items', line_items_path %>
        <%= nav_link 'Campaigns', campaigns_path %>
      <% end %>
    </div>

    <ul class="navbar-nav col-1">
      <li class="nav-item active">
        <% if current_user.present? %>
          <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger' %>
        <% else %>
          <%= link_to 'Log in', user_session_path, class: 'btn btn-outline-success' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
